# Example Integration

Template for an optional Linux integration. Disabled by default.

Copy this directory, rename it to your feature id, edit `feature.json`, and
flip `"enabled": true` to activate. This example ships no behavior — it only
documents the `feature.json` contract.
